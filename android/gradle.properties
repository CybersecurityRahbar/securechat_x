org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true

# Phase 1 compatibility: this project still applies the legacy Kotlin Gradle
# Plugin and AGP DSL. Keep the AGP 9 migration flags disabled until the
# Android build is intentionally migrated to the new interfaces.
android.newDsl=false
android.builtInKotlin=false
